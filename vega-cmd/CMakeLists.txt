SET(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -Wall -Wextra -D_GNU_SOURCE")

ADD_DEFINITIONS(-DVEGA_PPP_VERSION="${VEGA_PPP_VERSION}")

ADD_EXECUTABLE(vega-cmd
	vega_cmd.c
)

INSTALL(TARGETS vega-cmd
	RUNTIME DESTINATION bin
)
INSTALL(FILES vega-cmd.1
	DESTINATION share/man/man1
)
